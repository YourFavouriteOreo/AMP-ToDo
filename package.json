{
  "name": "AMP_Todo",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm run start",
    "complete-install-run": "npm install && cd client && npm install && cd .. && npm run dev",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "bcrypt": "^4.0.1",
    "body-parser": "^1.18.3",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^5.9.16",
    "nodemon": "^2.0.4"
  },
  "devDependencies": {
    "concurrently": "^4.1.2"
  }
}
